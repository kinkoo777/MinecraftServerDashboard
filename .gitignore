# dependencies
node_modules/

# the actual Minecraft server (jar, world, logs, player data) — never commit
mc-server/

# world backups
backups/

# machine-local state
config.json
schedules.json
console-logs/
*.log

# local installers
*.msi
*.exe

# test instance state
.test-config.json
.test-server/

reports.json
sessions.json

# playit.gg agent binary + its config
playit/

# cloudflared binary download
cloudflare/

# stray artifacts from local agent/swarm tooling
.claude-flow/
.swarm/
.remember/
ruvector.db
*.channel
v.id
